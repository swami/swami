;; -*- scheme -*-
;;
;; Note, everything should be sorted in here.

;; Boxed types ...

(define-boxed Range
  (in-module "Ipatch")
  (c-name "IpatchRange")
  (gtype-id "IPATCH_TYPE_RANGE")
  (fields
    '("gint" "low")
    '("gint" "high")
  )
)
